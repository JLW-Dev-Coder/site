# Dependencies
node_modules/

# Wrangler local state
.wrangler/

# SCALE prospects (contains real emails — never committed)
scale/prospects/*.csv
scale/prospects/archive/
scale/prospects/.batch-in-progress

# SCALE local working files (contain real emails — never committed)
scale/foia-master.json
scale/foia-master.updated.json
scale/ttmp-email1-pending.json